{
  "name": "sia-leaderboard-frontend",
  "version": "0.1.0",
  "description": "a frontend for the sia leaderboard",
  "main": "./dist/main.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Johnathan Howell",
  "license": "MIT",
  "devDependencies": {
    "babel-register": "^6.22.0",
    "chai": "^3.5.0",
    "eslint": "^3.14.0",
    "eslint-plugin-babel": "^4.0.1",
    "eslint-plugin-react": "^6.9.0",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "immutable": "^3.8.1",
    "react": "^15.4.2",
    "redux": "^3.6.0",
    "webpack": "^1.14.0"
  },
	"scripts": {
		"build": "webpack",
		"lint": "eslint --max-warnings 0 ./src ./test",
		"test-args": "mocha --compilers js:babel-register --recursive ./test",
		"test": "npm run lint && npm run build && npm run test-args"
	}
}
